import * as React from 'react';

import './style.less';

const prefixCls = 'menu-top-features';
const TopFeatures: React.SFC<{}> = () => (
    <div className={prefixCls}>
        sdafa
    </div>
);

export default TopFeatures;
