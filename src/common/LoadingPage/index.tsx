import * as React from 'react';

import './style.less';

const prefixCls = 'common-comp-loading';
const LoadingPage = () => (
    <div className={prefixCls}>loading</div>
);

export default LoadingPage;
